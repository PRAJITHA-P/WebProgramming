                          STEPS TO PREPARE HTML FILE
                          __________________________

1. 3 HTML webpages are created in visual studio code and linked them using hyperlinks
2. After creating structure,It is styled using CSS



                            GIT setup & GIT workflow
                            ________________________
   
   In github account a repository was created and git was downloaded and installed.
   After configuration,a local copy of the repository was created in workspace,using git clone command.
   All files,that is in the workspace are added to the index(staging area).
   After the git commit command, all files in the staging area are stored in the local repository,then
   it is pushed to remote repository
   
    
              
              
